/* if(all empty){
    - fetch logged users data;
}else if(regionSe,areaE,zoneE){
    - fetch users based on region and logged users area and zone
}else if(regionE,areaSelected,zoneE){
    - fetch users based on logged users region and area selected and logged users zone
}else if(regionE,areaE,zoneSe){
    - fetch users based on logged users region,area and zone selected
}
else if(regionSe,areaSelected,zoneE){
    - fetch users based on selected region and area and logged users zone
}else if(regionSe,areaE,zoneSe){
    - fetch users based on selected region and zone
}else if(regionE,areaSe,zoneSe){
    - fetch users based on logged users region, area selected and zone selected
} */